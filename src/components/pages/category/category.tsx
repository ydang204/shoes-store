import React, { Component } from 'react';

import './category.scss';

type Props = {};

type States = {};

class Category extends Component<Props, States> {
  constructor(props: Props) {
    super(props);
  }

  render() {
    return <p>category works!</p>;
  }
}

export default Category;
